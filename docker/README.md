# MoltGit - Docker

Dockerfile is found in the root of the repository.

Docker image can be found on [docker hub](https://hub.docker.com/r/moltgit/moltgit).

Documentation on using docker image can be found on [MoltGit Docs site](https://docs.gitea.com/installation/install-with-docker-rootless).
